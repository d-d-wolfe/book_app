<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <ul>
    <% newBook.forEach((value, index) =>{ %>
      <li key=<%= index %></li><h2><%= value.title %></h2>
          <h3><%= value.author %></h3>
          <h1>ISBN: <%= value.isbn %></h1>
          <p><%= value.description %></p>
          <img src=<%= value.image %> alt="">
          <button>Save this Book</button>
      </li>
    <% }) %>
  </ul>
  
</body>
</html>